                                    CCNA Routing and Switching Lab
                                    
                                    
                                    
*> You need Paceket tracer and GNS3
*> you can use 6.1 packet tracer or newer one

List of CCNA lab:

  Switching Topic:
    
    *> Vlan configuration
    *> VTP configuration
    *> STP configuration
    *> Etherchannel Configuration
    *> Port Security Configuration
    *> HSRP Configuration
    *> NAT configuration
    *> PAT configuration
    *> SSH configuration
    
  Router Topic:
     
     *> RIP configuration
     *> EIGRP configuration
     *> OSPF configuration
     *> OSPF ABR configuration
     *> OSPD ASBR configuraton
     *> ACL configuration
     *> IBGP configuration
     *> IBGP peergroup configuration
     *> EBGP configuration
     *> DHCP configuration


Download link:

   *> Packettracer : https://www.netacad.com/courses/packet-tracer
   *> GNS3 : https://www.gns3.com/software/download
   *> GNS3 CISCO IOS : https://i.srijit.com/gns3images
 
